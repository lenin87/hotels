import { ChnageTextDirective } from './chnage-text.directive';

describe('ChnageTextDirective', () => {
  it('should create an instance', () => {
    const directive = new ChnageTextDirective();
    expect(directive).toBeTruthy();
  });
});
